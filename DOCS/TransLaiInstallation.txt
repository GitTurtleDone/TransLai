git clone git@github.com:GitTurtleDone/TransLai.git
cd AI/TransLai
git checkout -b combined-app remotes/origin/combined-app
update conda
conda create --prefix ~/Programming/AI/TransLai/TransLaiPyEnv python=3.10.16 ipykernel
conda activate ~/Programming/AI/TransLai/TransLaiPyEnv
# check that python and pip are correct
which python
-> ~/Programming/AI/TransLai/TransLaiPyEnv/bin/python
which pip
-> ~/Programming/AI/TransLai/TransLaiPyEnv/bin/pip
python --version
-> Python 3.10.16
pip --version
-> pip 25.1.1

[submodule "whisper.cpp"]
	path = whisper.cpp
	url = https://github.com/ggml-org/whisper.cpp.git
[submodule "TTS"]
	path = TTS
	url = https://github.com/thinhlpg/TTS.git
	branch = add-vietnamese-xtts

# add whisper.cpp
git submodule add https://github.com/ggml-org/whisper.cpp.git
cd whisper cpp

(update 25/06/02) after cloning the project to Pi 5, git submodule update --init --recursive
manual copy ggml-base/tiny(.en).bin models in whisper.cpp/models
# install cmake
sudo apt-get update
sudo apt install cmake
# install c++ compiler
sudo apt update
sudo apt install build-essential

# build cpp project
cmake -B build
cmake --build build --config Release

# (check everything is working) output the transcribed speech to text file and select the language to be english
./build/bin/whisper-cli -m models/ggml-base.bin -f samples/jfk.mp3 -otxt -l en

# install ffmpeg to record sound
sudo apt update
sudo apt install ffmpeg

# install MarianMT

pip install transformers torch

pip install sentencepiece

pip install sacremoses #(optional for better translation)

# maybe no need -> change from transformers import MarianMTModel, MarianTokenizer to from transformer.models.marian import ...

# install vixtts
git submodule add -b add-vietnamese-xtts https://github.com/thinhlpg/TTS.git

# install tts from add-vietnamese-xtts
cd TTS
pip install -e ./TTS

pip uninstall tensorflow # tensorflow was installed by wrong pip so it was removed after that from transformers import MarianMTModel, MarianTokenizer works

add vixtts-demo to .gitignores

Manually copy model.pth vocab.json and config.json in vixtts-deom/model from the laptop to the TransLai/vixtts-demo/model of the desktop


Maybe also need
pip install -q vinorm==2.0.7
pip install -q cutlet
pip install -q unidic==1.1.0
pip install -q underthesea (couldn't install in Pi 5, because Pi 5 used ARM-based architure, on common platforms x86/64 linux, MacOS, and Windows, many python packages provide wheel files, these wheels have compiled Rust extention, 
-> no build process needed, 
ARM64 of pi doesn't have wheel -> pyproject.tom needed
need to go 
1) curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
2) source "$HOME/.cargo/env"
3) pip install maturin==0.14.1 (the updated version 1.8.xx didn't work need to downgrade to 0.14.1)
4) git clone https://github.com/undertheseanlp/underthesea.git
5) cd underthesea/extensions/underthesea_core
6) change the file pyproject.toml to:
[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "underthesea_core"
version = "1.0.4"
description = "Underthesea Core"
authors = [
    { name = "Giang T Dang", email = "giang2110@gmail.com" }
]
requires-python = ">=3.7,<3.12"
readme = "README.md"
#license = { file = "LICENSE" }
7) under underthesea_core run:
maturin develop --release
8) go back to underthesea run:
pip install -e . --config-settings editable_mode=compat (without compatible mode also didn't work)



then after may be update pip wheel )
pip install -q gradio==4.35 (not installed in  Pi 5)
pip install deepfilternet==0.5.6 # this doesn't work

pip install fastapi uvicorn
pip install torchaudio==2.7.0
for the packages installed by the wrong pip

sudo apt install -y nodejs
cd ReactTSFE
npm install 
npm run dev

pip install ipython


When going back to laptop computer it didn't work because laptop ran ubuntu 20.04 why desktop runs 22.04.5
fixed by upgrading wsl2 ubuntu with 
sudo apt update
sudo apt upgrade
sudo do-release-upgrade


# cd ~/Prorgamming
cd ~/Programming/AI/TransLai
code .
conda activate ~/Programming/AI/TransLai/TransLaiPyEnv

nano ~/.bash_profile
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion


# install pulse audio
sudo apt update
sudo apt install pulseaudio-utils

After install on the desktop, npm run dev stop working because incorrect node and npm were used windows is used.

the fix was to 
delete global node of WSL2:

sudo apt remove nodejs


and remove the path to npm windows
export PATH=$(echo "$PATH" | tr ':' '\n' | grep -v "/mnt/c/Program Files/nodejs" | paste -sd:)
export PATH=$(echo "$PATH" | tr ':' '\n' | grep -v "/mnt/c/Users/gtd19/AppData/Roaming/npm" | paste -sd:)
export PATH=$(echo "$PATH" | tr ':' '\n' | grep -v "/mnt/c/Program Files/nodejs/npm" | paste -sd:)


Move cd cond activate and code . back to this order seem to fix everything

# cd ~/Prorgamming
cd ~/Programming/AI/TransLai
code .
conda activate ~/Programming/AI/TransLai/TransLaiPyEnv

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

250610
sudo apt update
sudo apt install git
git --version
mkdir Programming
cd Programming
mkdir AI

cd AI
git clone to clone TransLai
-> git add modules to install seeed-voicecard (see Installation of seed studio reSpeaker 2-mic pi hat
Installation on Pi 4

cd ~
wget https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-aarch64.sh
bash Miniforge3-Linux-aarch64.sh
yes

Restart the terminal
cd Programming/AI/TransLai
conda create --prefix ~/Programming/AI/TransLai/TransLaiPyEnv python=3.10.16 ipykernel
conda activate /home/giang/Programming/AI/TransLai/TransLaiPyEnv

# update submodules
git submodule update --init --recursive
failed at Username for 'https://huggingface.co': ^C when trying to update underthesea.
(may or may not needed) git submodule init whisper.cpp
git submodule update --init whisper.cpp
Manually copy the models ggml-base/tiny(.en).bin models in whisper.cpp/models
# install cmake
sudo apt-get update
sudo apt install cmake
# install c++ compiler
sudo apt update
sudo apt install build-essential

# build cpp project
cmake -B build
cmake --build build --config Release

# (check everything is working) output the transcribed speech to text file and select the language to be english
./build/bin/whisper-cli -m models/ggml-base.bin -f samples/jfk.mp3 -otxt -l en

# install ffmpeg to record sound
sudo apt update
sudo apt install ffmpeg

# install MarianMT

pip install update

python -m pip install --upgrade pip

pip install transformers torch (no errors after update, upgrade) --> transformers 4.52.4 didn't work, but 4.51.3 works
need to 
pip install transformers==4.51.3 torch

pip install sentencepiece

pip install sacremoses #(optional for better translation)


# install vixtts
----didn't work-------
git submodule init TTS
git submodule update --remote --checkout TTS
cd TTS 
git checkout add-vietnamese-xtts
git pull origin add-vietnamese-xtts

cd ~/Programming/AI/TransLai/TTS
check with "git status" that:
"On branch add-vietnamese-xtts
Your branch is up to date with 'origin/add-vietnamese-xtts'."
----didn't work----------

git submodule deinit -f TTS
git rm -f TTS
rm -rf .git/modules/TTS

# install vixtts
git submodule add -b add-vietnamese-xtts https://github.com/thinhlpg/TTS.git

# install tts from add-vietnamese-xtts
pip install -e ./TTS



Manually copy model.pth vocab.json and config.json in vixtts-demo/model from the laptop to the TransLai/vixtts-demo/model of the desktop


Maybe also need
pip install -q vinorm==2.0.7
pip install -q cutlet
pip install -q unidic==1.1.0
pip install -q underthesea (couldn't install in Pi 5, because Pi 5 used ARM-based architure, on common platforms x86/64 linux, MacOS, and Windows, many python packages provide wheel files, these wheels have compiled Rust extention, 
-> no build process needed, 
ARM64 of pi doesn't have wheel -> pyproject.tom needed
need to go 
1) curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
2) source "$HOME/.cargo/env"
3) pip install maturin==0.14.1 (the updated version 1.8.xx didn't work need to downgrade to 0.14.1)
4) it submodule update --init underthesea
5) cd underthesea/extensions/underthesea_core
6) change the file pyproject.toml to:
[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "underthesea_core"
version = "1.0.4"
description = "Underthesea Core"
authors = [
    { name = "Vu Anh", email = "anhv.ict91@gmail.com" },
    { name = "Giang T Dang", email = "giang2110@gmail.com" }
]
requires-python = ">=3.7,<3.12"
readme = "README.md"
#license = { file = "LICENSE" }
7) under underthesea_core run:
maturin develop --release
8) go back to underthesea run:
pip install -e . --config-settings editable_mode=compat (without compatible mode also didn't work)


pip install fastapi uvicorn torchaudio==2.7.1

sudo apt update && sudo apt upgrade

sudo apt install code -y

#install node
sudo apt install -y nodejs

#install npm
npm install --> didn't work because nmp is not installed by default in Pi 4 therefore 
	sudo apt install -y npm
	npm --version to check 
# install nvm
cd ~
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc   # or ~/.zshrc if you're using zsh

# after that install node and npm again with 
nvm install --lts (this will install node and npm automatically, don't have to do them manually like previouse steps)


# run react project
cd ReactTSFE
npm install
npm run dev



# adding to .bashrc for convenience
cd ~/Programming/AI/TransLai
code .
conda activate ~/Programming/AI/TransLai/TransLaiPyEnv


pip install ipython
# install pulse audio
sudo apt update
sudo apt install pulseaudio-utils


Combination of packages that works on pi 4:


(/home/giang/Programming/AI/TransLai/TransLaiPyEnv) giang@raspberrypi:~/Programming/AI/TransLai $ pip list
Package                 Version     Editable project location
----------------------- ----------- -----------------------------------------------
absl-py                 2.3.0
aiohappyeyeballs        2.6.1
aiohttp                 3.12.11
aiosignal               1.3.2
annotated-types         0.7.0
anyascii                0.3.2
anyio                   4.9.0
asttokens               3.0.0
async-timeout           5.0.1
attrs                   25.3.0
audioread               3.0.1
babel                   2.17.0
bangla                  0.0.5
blinker                 1.9.0
blis                    1.2.1
bnnumerizer             0.0.2
bnunicodenormalizer     0.1.7
catalogue               2.0.10
certifi                 2025.4.26
cffi                    1.17.1
charset-normalizer      3.4.2
click                   8.2.1
cloudpathlib            0.21.1
comm                    0.2.2
confection              0.1.5
contourpy               1.2.1
coqpit                  0.0.17
cutlet                  0.5.0
cycler                  0.12.1
cymem                   2.0.11
Cython                  3.1.2
dateparser              1.1.8
debugpy                 1.8.14
decorator               5.2.1
docopt                  0.6.2
einops                  0.8.1
encodec                 0.1.1
exceptiongroup          1.3.0
executing               2.2.0
fastapi                 0.115.12
filelock                3.18.0
Flask                   3.1.1
fonttools               4.58.2
frozenlist              1.7.0
fsspec                  2025.5.1
fugashi                 1.5.1
g2pkk                   0.1.2
grpcio                  1.73.0
gruut                   2.2.3
gruut-ipa               0.13.0
gruut_lang_de           2.0.1
gruut_lang_en           2.0.1
gruut_lang_es           2.0.1
gruut-lang-fr           2.0.2
h11                     0.16.0
hangul-romanize         0.1.0
hf-xet                  1.1.3
huggingface-hub         0.32.4
idna                    3.10
importlib_metadata      8.7.0
inflect                 7.5.0
ipykernel               6.29.5
ipython                 8.37.0
itsdangerous            2.2.0
jaconv                  0.4.0
jamo                    0.4.1
jedi                    0.19.2
jieba                   0.42.1
Jinja2                  3.1.6
joblib                  1.5.1
jsonlines               1.2.0
jupyter_client          8.6.3
jupyter_core            5.8.1
kiwisolver              1.4.8
langcodes               3.5.0
language_data           1.3.0
lazy_loader             0.4
librosa                 0.10.0
llvmlite                0.43.0
marisa-trie             1.2.1
Markdown                3.8
markdown-it-py          3.0.0
MarkupSafe              3.0.2
matplotlib              3.8.4
matplotlib-inline       0.1.7
maturin                 0.14.1
mdurl                   0.1.2
mojimoji                0.0.13
more-itertools          10.7.0
mpmath                  1.3.0
msgpack                 1.1.0
multidict               6.4.4
murmurhash              1.0.13
mutagen                 1.47.0
nest_asyncio            1.6.0
networkx                2.8.8
nltk                    3.9.1
num2words               0.5.14
numba                   0.60.0
numpy                   1.22.0
packaging               25.0
pandas                  1.5.3
parso                   0.8.4
pexpect                 4.9.0
pickleshare             0.7.5
pillow                  11.2.1
pip                     25.1.1
plac                    1.4.5
platformdirs            4.3.8
pooch                   1.8.2
preshed                 3.0.10
prompt_toolkit          3.0.51
propcache               0.3.2
protobuf                6.31.1
psutil                  7.0.0
ptyprocess              0.7.0
pure_eval               0.2.3
pycparser               2.22
pydantic                2.11.5
pydantic_core           2.33.2
Pygments                2.19.1
pynndescent             0.5.13
pyparsing               3.2.3
pypinyin                0.54.0
pysbd                   0.3.4
python-crfsuite         0.9.11
python-dateutil         2.9.0.post0
pytz                    2025.2
PyYAML                  6.0.2
pyzmq                   26.4.0
regex                   2024.11.6
requests                2.32.4
rich                    14.0.0
sacremoses              0.1.1
safetensors             0.5.3
scikit-learn            1.7.0
scipy                   1.11.4
sentencepiece           0.2.0
setuptools              80.9.0
shellingham             1.5.4
six                     1.17.0
smart_open              7.1.0
sniffio                 1.3.1
soundfile               0.13.1
soxr                    0.5.0.post1
spacy                   3.8.7
spacy-legacy            3.0.12
spacy-loggers           1.0.5
srsly                   2.5.1
stack_data              0.6.3
starlette               0.46.2
style                   1.1.0
SudachiDict-core        20250515
SudachiPy               0.6.10
sympy                   1.14.0
tensorboard             2.19.0
tensorboard-data-server 0.7.2
thinc                   8.3.4
threadpoolctl           3.6.0
tokenizers              0.21.1
tomli                   2.2.1
torch                   2.7.1
torchaudio              2.7.1
tornado                 6.5.1
tqdm                    4.67.1
trainer                 0.0.36
traitlets               5.14.3
transformers            4.51.3
TTS                     0.22.0      /home/giang/Programming/AI/TransLai/TTS
typeguard               4.4.3
typer                   0.16.0
typing_extensions       4.14.0
typing-inspection       0.4.1
tzlocal                 5.3.1
umap-learn              0.5.7
underthesea             6.8.4       /home/giang/Programming/AI/TransLai/underthesea
underthesea_core        1.0.4
Unidecode               1.4.0
unidic                  1.1.0
update                  0.0.1
urllib3                 2.4.0
uvicorn                 0.34.3
vinorm                  2.0.7
wasabi                  0.10.1
wcwidth                 0.2.13
weasel                  0.4.1
Werkzeug                3.1.3
wheel                   0.45.1
wrapt                   1.17.2
yarl                    1.20.1
zipp                    3.23.0

